# 3音场与15音场理论备忘录

本文整理截至 2026-09-14 的讨论，为以后重写功能和声、旋律中心判断和进行生成提供理论输入。这里的“3音”“15音”指有理比例中的谐波整数及其 EDO 映射，不是传统和弦三度、音阶第三级或第十五级。

现有 [`three_tone.py`](three_tone.py) 处理的是**单个和弦的局部3音分类**；本文主要讨论由候选3音或15音诱导出的**音阶级音场**。两层概念有关，但不能互相替代。

## 1. 统一的 val 定义

设当前音律为 `N-EDO`，其 patent val 为 `v_N(n)`。候选中心 `t` 可以是 EDO 中任意音级，不要求位于目标音阶内。

若整数集合为 `H`，由 `t` 诱导的音场为：

```text
F_H(t) = { (t + v_N(h) - v_N(c)) mod N | h in H }
```

其中 `c` 是中心整数：3音场取 `c=3`，15音场取 `c=15`。这一公式直接使用 patent val；不能先把 cents 各自四舍五入后再拼接。

计算中心强度时，至少要保留以下互相独立的数据：

1. `field_pcs`：完整诱导音场，包括音阶外音。
2. `intersection_pcs`：诱导场与目标音阶的交集。
3. `missing_pcs`：诱导场中缺失的音。
4. `center_in_scale`：中心本身是否在音阶内。
5. `kernel_members`：核心大小调结构是否完整。
6. `activated_members`：外围音是否能与核心音形成紧凑的低限结构。
7. `perceptual_prior`：音区、持续、旋律位置和实际试听形成的中心性先验。

交集数量是第一层指标，不应直接等同于功能强度。

## 2. 3音场

### 2.1 核心七音组

3音关联的整数集合为：

```text
H3 = {1, 3, 5, 7, 9, 15, 21}
```

相对于整数 3，并约入同一八度后得到：

```text
F3 ratios = {1/1, 7/6, 5/4, 4/3, 3/2, 5/3, 7/4}
          = 12:14:15:16:18:20:21
```

它也可以看作从 12-AFDO 中截取出的七个 7 奇数限音。3音作为中心，比只观察整数 1 能直接关联更多低限成员：整数 3 与 `1, 3, 5, 7, 9, 15, 21` 形成简单关系。

大调型核心结构暂记为：

```text
K3 = {1/1, 5/4, 3/2}
```

`K3` 完整时有很直接的大三和弦定调能力，但它不是中心性的唯一来源。天干音阶中的辛是重要反例：辛场缺少 `3/2`，仍可因覆盖度、音区听感和双场结构形成强中心。

### 2.2 9、11 奇数限的弱扩展

讨论过的弱关联为：

| 原整数 | 相对3音的八度归一关系 | 处理建议 |
|---:|---:|---|
| 27 | 9/8 | 弱关联 |
| 11 | 11/6 | 谨慎的弱关联 |
| 33 | 11/8 | 不计入正向核心成员 |

`11/8` 的特殊听感可能破坏3音核心，因此不能仅因 33 含因子 3 就把它自动奖励。这里应明确区分“整数算术上与3有关”和“听觉上支持3音中心”。13 奇数限及以上暂时忽略。

### 2.3 自然大调的双3音场解释

对自然大调：

```text
{1/1, 9/8, 5/4, 4/3, 3/2, 5/3, 15/8}
```

以 I 为3音中心时，交集为：

```text
{I, III, IV, V, VI}
```

以 V 为3音中心时，交集为：

```text
{I, II, III, V, VII}
```

二者各命中五音，共同部分为 `{I, III, V}`，并集覆盖完整七声音阶。这给出了自然大调主音—属音双中心以及大三和弦核心的一种3音场解释。

## 3. 15音场

### 3.1 核心十音组

15音关联的整数集合为：

```text
H15 = {3, 5, 9, 15, 21, 25, 35, 45, 75, 105}
```

相对于整数 15 的原始关系为：

```text
{1/5, 1/3, 3/5, 1/1, 7/5, 5/3, 7/3, 3/1, 5/1, 7/1}
```

约入同一八度并排序后为：

```text
F15 ratios = {1/1, 7/6, 6/5, 5/4, 4/3,
              7/5, 3/2, 8/5, 5/3, 7/4}
```

小调型核心结构为：

```text
K15 = {1/1, 6/5, 3/2}
```

15音场的有效强度尤其依赖 `K15` 以及外围音与 `6/5` 的组合。高交集但缺少 `6/5`，不一定构成强15音中心。

### 3.2 三种小调的双15音场解释

自然小调中，以 I 为15音中心的交集为：

```text
{I, bIII, IV, V, bVI}
```

以 V 为15音中心的交集为：

```text
{I, II, bIII, V, bVII}
```

两场共同部分是 `{I, bIII, V}`，即小三和弦核心，并集覆盖自然小调七个音。

和声小调将 V 场一侧的 `bVII` 换成 `VII`；这里所说的上行旋律小调同时让 I 场一侧使用 `VI`，而 V 场一侧使用 `VII`：

| 音阶 | I 中心15音场交集 | V 中心15音场交集 |
|---|---|---|
| 自然小调 | I, bIII, IV, V, bVI | I, II, bIII, V, bVII |
| 和声小调 | I, bIII, IV, V, bVI | I, II, bIII, V, VII |
| 旋律小调 | I, bIII, IV, V, VI | I, II, bIII, V, VII |

三者都保留 `bIII`。如果把它改成还原 III，核心便转向大调；同时保留两者则更接近大小调混合。

## 4. 天干音阶的3音场

天干音阶为：

```text
N = 72
甲0 乙7 丙16 丁23 戊30 己35 庚42 辛49 壬58 癸65
```

72-EDO 中3音七音组相对中心的音级偏移为：

```text
{0, 16, 23, 30, 42, 53, 58}
```

遍历全部 72 个候选中心，交集多于三个音的结果为：

| 3音中心 | 是否在音阶内 | 交集数 | 交集 |
|---|---|---:|---|
| 甲（0） | 是 | 6 | 甲、丙、丁、戊、庚、壬 |
| 辛（49） | 是 | 6 | 甲、乙、戊、己、辛、癸 |
| 乙（7） | 是 | 5 | 乙、丁、戊、辛、癸 |
| 庚（42） | 是 | 5 | 甲、丁、庚、壬、癸 |
| 第19步 | 否 | 4 | 甲、己、庚、辛 |
| 己（35） | 是 | 4 | 丙、己、壬、癸 |
| 癸（65） | 是 | 4 | 丙、丁、己、癸 |

全部 72 个候选中心的交集数量分布为：

```text
0音:44, 1音:10, 2音:7, 3音:4, 4音:3, 5音:2, 6音:2
```

### 4.1 甲—辛双3音场

最强覆盖的甲场与辛场分别是：

| 相对关系 | 甲中心 | 辛中心 |
|---:|---|---|
| 1/1 | 甲 | 辛 |
| 7/6 | 丙 | 癸 |
| 5/4 | 丁 | 甲 |
| 4/3 | 戊 | 乙 |
| 3/2 | 庚 | 第19步，缺失 |
| 5/3 | 第53步，缺失 | 戊 |
| 7/4 | 壬 | 己 |

因此：

```text
（F3(甲) ∩ 天干）∪（F3(辛) ∩ 天干）= 完整天干十音
F3(甲) ∩ F3(辛) = {甲, 戊}
```

第一式说的是两个场在目标音阶内的覆盖。完整的甲场还含音阶外第53步，完整的辛场还含音阶外第19步。

可以把十音分成：

- 甲中心特征音：丙、丁、庚、壬。
- 共享枢纽音：甲、戊。
- 辛中心特征音：乙、己、辛、癸。

甲缺 `5/3`，辛缺 `3/2`，两场覆盖度同为 `6/7`。甲具有完整 `K3={甲,丁,庚}`，定调方式更接近完整大三和弦；辛没有上方纯五度，但实际低音区的辛具有明显主音感，并由其余成员构成另一种稳定色彩。因而后续实现应把“覆盖度势均力敌”和“核心结构并不对称”同时保留下来。

共享的甲—戊在甲场中对应 `1/1–4/3`，在辛场中对应 `5/4–5/3`，两种解释内部都保持约 `4/3`，适合担当场切换枢纽。

两场还有方向不对称：辛场包含甲，甲在辛场中是 `5/4`；甲场不包含辛。因此辛中心可以持续保留甲而维持一定歧义，辛转甲时也可把已有的甲重新解释为中心。甲转辛通常需要引入辛本身及乙、己、癸等辛场特征音，换色更明显。

癸是辛场的 `7/6` 特征音，却不属于甲场。癸在甲中心区域落在重拍、长时值或结构性和弦中时，容易破坏甲中心或把听感引向辛场；短促经过或解决用途不必一律禁止。

第19步虚拟3音命中 `{辛,甲,己,庚}`。这些音密集出现时，可能让旋律与和声产生“缺少关键中心音”的感觉；它仍应作为音阶外中心保存，而不是标记成无法分类。

### 4.2 乙—庚副3音轴

甲—辛构成覆盖整个天干音阶的主轴，各命中五音的乙场与庚场则构成副轴：

| 相对关系 | 乙中心 | 庚中心 |
|---:|---|---|
| 1/1 | 乙 | 庚 |
| 7/6 | 丁 | 壬 |
| 5/4 | 戊 | 癸 |
| 4/3 | 第37步，缺失 | 甲 |
| 3/2 | 辛 | 第12步，缺失 |
| 5/3 | 第60步，缺失 | 丁 |
| 7/4 | 癸 | 第28步，缺失 |

乙场实际命中 `{乙,丁,戊,辛,癸}`，缺 `4/3、5/3`；庚场实际命中 `{甲,丁,庚,壬,癸}`，缺 `3/2、7/4`。两场各命中 `5/7`，比主轴两场的 `6/7` 略弱。

副中心与主中心的交集呈现明确的两组归属：

| 场组合 | 共同音数 | 共同音 |
|---|---:|---|
| 甲—庚 | 4 | 甲、丁、庚、壬 |
| 辛—乙 | 4 | 乙、戊、辛、癸 |
| 甲—乙 | 2 | 丁、戊 |
| 辛—庚 | 2 | 甲、癸 |

因此庚是甲侧副中心，乙是辛侧副中心。它们都能保留所属主场的大部分材料，只替换或重新解释少量音便形成新的中心感。这种低成本再解释，是副轴与主轴竞争的第一项能力。

乙场具有完整的场级大调核心：

```text
{乙, 戊, 辛} = {1/1, 5/4, 3/2}
```

所以乙虽然总覆盖少一音，却有清楚的中心、三度和五度，可形成集中的大调型局部区域。相同音级集合在现有和弦词汇中以 `辛乙戊=3:4:5` 的自身比例出现，并由和弦级引擎归入辛3音；这正说明场级中心和和弦自身3音不能共用一个字段。乙的优势是场结构完整，局限是缺少 `4/3、5/3`，向外展开的中层支撑较少。

庚场没有自身的 `3/2`，场级大调核心不完整，但它有三项补偿：

1. 庚本身是甲场的 `3/2`，在甲中心音乐中原本就是高度显著的结构音。
2. `{庚,甲,丁}=3:4:5` 是现有词汇中的紧凑和弦，其和弦自身3音就是庚，能直接支持庚中心。
3. 当前32和弦中，庚3音组有7个和弦，其中6个为 `present`；乙3音组有4个，其中3个为 `present`。庚拥有更丰富的实际和声实现路径。

因此乙的主要竞争力是**完整场核心**，庚的主要竞争力是**结构音显著性和和弦词汇支持**。两者可以达到相近的中心感，但来源不同，后续评分不应只看是否具有完整 `K3`。

乙场与庚场的关系为：

```text
（F3(乙) ∩ 天干）∩（F3(庚) ∩ 天干）= {丁, 癸}
（F3(乙) ∩ 天干）∪（F3(庚) ∩ 天干）
  = {甲,乙,丁,戊,庚,辛,壬,癸}
```

副轴共同音丁—癸构成 72-EDO 第42步的纯五度类关系。在乙场中它们是 `7/6、7/4`，比值为 `3/2`；在庚场中是 `5/3、5/4`，彼此为 `4/3`。这是一个很稳定的换场骨架，但它不含乙或庚中心本身，所以更擅长维持歧义和连接，不像甲—辛主轴的共同音中直接包含甲中心。

乙与庚相隔第35步，对应约 `7/5`；而且乙不在庚场、庚也不在乙场。副轴因此比主轴更对称、更互斥：主轴存在“辛场包含甲、甲场不包含辛”的方向层级，副轴没有这种吸收关系。乙—庚切换更容易产生明显的竞争或换色，需要通过丁—癸共同骨架完成，而不是直接保留其中一个中心。

副轴并集缺少丙、己，正好分别失去甲场的 `7/6` 色彩和辛场的 `7/4` 色彩。它覆盖八音，不能像甲—辛主轴一样解释全部天干材料。因此更适合承担：

- 主中心内部的局部再定中心：甲到庚、辛到乙。
- 乐句中段、展开段和顺序模进中的对照中心。
- 通过丁—癸纯五度骨架进行的模糊过渡。
- 从一个主场借入另一个主场特征音：乙场借入丁，庚场借入癸。

主轴负责全局统摄，副轴的竞争力来自更集中的核心、既有和弦的重新解释以及更鲜明的双极换色。

### 4.3 己—癸纯5限轴

己场与癸场各只命中四个3音场成员，但命中的关系全部属于纯5限：

| 相对关系 | 己中心 | 癸中心 |
|---:|---|---|
| 1/1 | 己 | 癸 |
| 7/6 | 第51步，缺失 | 第9步，缺失 |
| 5/4 | 壬 | 丙 |
| 4/3 | 癸 | 丁 |
| 3/2 | 第5步，缺失 | 己 |
| 5/3 | 丙 | 第46步，缺失 |
| 7/4 | 第21步，缺失 | 第51步，缺失 |

两场都缺少 `7/6、7/4`，所以不依靠7限色彩建立中心：

```text
F3(己) ∩ 天干 = {丙,己,壬,癸}
F3(癸) ∩ 天干 = {丙,丁,己,癸}
```

己到癸是 `4/3`，癸到己是 `3/2`。两个中心互相位于对方场内，这与前两条轴不同：

- 甲—辛主轴只有辛场包含甲，具有方向不对称。
- 乙—庚副轴彼此都不包含，竞争更明显。
- 己—癸轴彼此都包含，更偏向合作、互释和内部循环。

两场的交集与并集为：

```text
（F3(己) ∩ 天干）∩（F3(癸) ∩ 天干）= {丙,己,癸}
（F3(己) ∩ 天干）∪（F3(癸) ∩ 天干）= {丙,丁,己,壬,癸}
```

共享的 `{丙,己,癸}` 是这个小生态的核心。以己为参照：

```text
己、癸、丙 = {1/1, 4/3, 5/3} = 3:4:5
```

以癸为参照并重新安排八度：

```text
癸、丙、己 = {1/1, 5/4, 3/2} = 4:5:6
```

因此同一组三个音可以在己的 `3:4:5` 谐波结构与癸的大三和弦结构之间切换。中心变化不一定需要替换音符，只需改变低音、声部排列、重拍归属和后续解决方向。现有词汇中的 `己癸丙=3:4:5` 正好提供了这个核心。

两场各有一个侧面特征音：

- 壬是己场的 `5/4`，不在癸场内，强化己侧。
- 丁是癸场的 `4/3`，不在己场内，强化癸侧。

完整五音生态相对癸可写成：

```text
癸、丙、丁、己、壬 = {1/1, 5/4, 4/3, 3/2, 15/8}
```

相对己则为：

```text
己、壬、癸、丙、丁 = {1/1, 5/4, 4/3, 5/3, 16/9}
```

两种写法都只含素数2、3、5。它虽然只覆盖五个天干音，却拥有较高的内部连通度、完整的共同三音核心和明确的两侧特征音。按低限纯度加权后，它的有效中心性可以明显高于原始 `4/7` 覆盖率所暗示的水平。

己与癸的优势也互相补偿：癸场具有完整场级 `K3={癸,丙,己}`，但当前和弦级癸3音组只有 `癸丁己` 一个和弦；己场缺少自身 `3/2`，却有3个和弦级己3音和弦，包括核心 `己癸丙=3:4:5`。因此癸主要依靠场结构完整度，己主要依靠实际低限和弦支持。

这条轴与甲、辛主场的交集数量相当平衡：己场分别与甲场、辛场共享两音，癸场也分别与甲场、辛场共享两音。五音生态本身不含甲、辛或主轴枢纽戊，可以在局部暂时悬置主轴引力，形成独立而清澈的5限区域。

生成时可把它用于：

- 从含7限色彩的主轴区域进入较纯净的5限段落。
- 保持 `{丙,己,癸}`，仅用壬或丁表明当前偏向己场还是癸场。
- 通过低音和重拍重解释完成己—癸平滑换中心。
- 用癸场的 `4:5:6` 获得明确大调型稳定，再用己场的 `3:4:5` 获得更谐波化的悬浮色彩。

后续场强评分应增加 `prime_limit_purity` 或等价的低限纯度特征，否则单纯按交集数量会系统性低估己—癸轴。

### 4.4 385/384 与稀有的11限局部桥梁

天干音阶的常规结构以3、5、7限为主，但11限可以在少数特定声部关系中发挥作用。已在实际退火结果中观察到：

```text
低2甲 — 低丙 — 丙 — 高乙
```

其中低2甲是 bass 的延续音，低丙位于其上方 `7/3`，而：

```text
低丙 : 丙 : 高乙 = 3:6:11
```

从低2甲沿这条链到高乙得到：

```text
(7/3) × (11/3) = 77/9
```

天干音阶中甲到乙约为 `16/15`；跨三个八度为 `128/15`。两种路径之差正是：

```text
(77/9) / (128/15) = 385/384
```

`385/384` 约为 4.503 音分，并被72-EDO patent val精确消去：

```text
v72(385/384) = 0 mod 72
```

也可以在音级类内写成：甲—丙为 `7/6`，丙—乙为 `11/6`，二者相乘并约入八度得到 `77/72`；它与甲—乙的 `16/15` 仍相差 `385/384`。因此同一个72-EDO音级结构可以同时获得5限直接解释和7限—11限链式解释。

可用的低音—内声—旋律关系不是任意组合，而是以下几类：

| 延续 bass | 内声支撑 | bass—内声 | 上方旋律 | 内声—旋律音级关系 |
|---|---|---:|---|---:|
| 甲 | 丙 | 7/6 | 乙 | 11/6；复音程可为11/3 |
| 甲 | 丙 | 7/6 | 辛 | 11/8；复音程可为11/4 |
| 甲 | 壬 | 7/4 | 乙 | 11/9 |
| 甲 | 壬 | 7/4 | 辛 | 11/6；复音程可为11/3 |
| 戊 | 丙 | 7/4 | 乙或辛 | 分别由丙形成11/6或11/8 |
| 庚 | 壬 | 7/6 | 乙或辛 | 分别由壬形成11/9或11/6 |

这里的共同原则是：bass 与丙、壬之间先有简单7限支撑，丙、壬再把旋律乙、辛解释成 `11/3、11/4、11/6、11/9` 等关系；通过实际音区安排，让含整数11的音位于上方泛音侧。11不是新的全局中心，而是连接低限 bass 与较难配和声的乙、辛旋律音的局部上方泛音。

这不推翻前面对 `11/8` 的谨慎处理。`11/8` 不应加入3音场的常规正向核心，但丙—辛的 `11/8` 可以在以下限制同时成立时成为有效局部边：

- 11位于上方旋律侧，而不是低音或下方生成关系。
- 下方丙由甲或戊的7限关系支持。
- bass 是先前已经发声的延续音，不与整个11限结构同时强起音。
- 该结构是短暂的和声解释，不连续占据多个结构重拍。

“bass不同时起音”很重要。当前退火已经分别计算 `sounding` 与 `attack`：延续 bass 参加持续发声的整体CSE，却不会加入新时刻的同时起音集合。这使完整四音关系可以作为背景融合，同时避免四个声部共同起音时过强、过陌生的11限攻击感。该机制与实际出现并且听感良好的结果一致。

后续代码不应建立固定的385/384和弦模板，也不应加入全局 `weight_11` 奖励。更合适的处理是：

1. 先增加只读诊断，识别“延续 bass + 7限内声 + 上方11限旋律”的发声切片。
2. 保留现有CSE和退火自然发现这种结构的能力，避免规则提前排除丙、壬。
3. 如果旋律乙、辛的自动配和声仍明显困难，只给上述条件化结构很小的候选保留或平分奖励。
4. 奖励按乐句限频，并要求11在上方声部；不能泛化成所有11限音程都更好。
5. 分别记录同时起音与延续发声中的出现率和CSE，进行消融试听。

这类结构的价值来自罕见、声部方向正确和多条低限关系同时闭合。频繁主动插入会消耗它的特殊效果，也可能重新造成此前担心的11限音程破坏3音中心。

## 5. 天干音阶的15音场

72-EDO 中15音十音组相对中心的音级偏移为：

```text
{0, 16, 19, 23, 30, 35, 42, 49, 53, 58}
```

遍历全部 72 个候选中心，交集多于三个音的结果为：

| 15音中心 | 是否在音阶内 | 交集数 | 交集 |
|---|---|---:|---|
| 甲（0） | 是 | 8 | 甲、丙、丁、戊、己、庚、辛、壬 |
| 乙（7） | 是 | 6 | 乙、丁、戊、庚、辛、癸 |
| 戊（30） | 是 | 6 | 甲、乙、丙、戊、辛、癸 |
| 辛（49） | 是 | 6 | 甲、乙、戊、己、辛、癸 |
| 丁（23） | 是 | 5 | 甲、丁、庚、壬、癸 |
| 庚（42） | 是 | 5 | 甲、丁、庚、壬、癸 |
| 癸（65） | 是 | 5 | 丙、丁、己、庚、癸 |
| 丙（16） | 是 | 4 | 丙、己、壬、癸 |
| 第19步 | 否 | 4 | 甲、己、庚、辛 |
| 己（35） | 是 | 4 | 丙、己、壬、癸 |
| 第53步 | 否 | 4 | 甲、丙、丁、戊 |

完整的交集数量分布为：

```text
0音:38, 1音:11, 2音:4, 3音:8, 4音:4,
5音:3, 6音:3, 7音:0, 8音:1
```

### 5.1 甲不是强15音中心

甲15音场虽然命中八音，但缺少：

```text
6/5 -> 第19步
5/3 -> 第53步
```

其中 `6/5` 缺失使小调核心 `K15={1,6/5,3/2}` 不完整。甲15音场相对甲3音场名义上增加：

```text
己 = 7/5
辛 = 8/5
```

但没有 `6/5` 时，己不能参与特征性的 `{1,6/5,7/5}=5:6:7` 结构，因此其15音意义没有被充分激活。辛的 `8/5` 仍是可用扩展。这里不是说己在所有甲中心和声中都无意义；它仍可形成 `{1,7/5,3/2}=10:14:15`，只是这不是完整的15音小调核心。

结论是：甲是强3音中心，但不能仅凭 `8/10` 的原始交集判为强15音中心。

### 5.2 戊是强15音中心

戊3音场只命中 `{戊,甲,丙}`，缺少 `5/4`，不是强3音中心。戊15音场则命中：

```text
戊 = 1/1
辛 = 6/5
甲 = 3/2
癸 = 7/5
乙 = 8/5
丙 = 7/4
```

它具有完整小调核心，并激活三个相对戊3音场新增的音：

```text
戊辛甲 = {1, 6/5, 3/2} = 10:12:15
戊辛癸 = {1, 6/5, 7/5} = 5:6:7
戊辛乙 = {1, 6/5, 8/5} = 5:6:8
```

因此戊是强15音中心而不是强3音中心。

### 5.3 戊小调与甲属中心

相对戊，天干音阶提供：

```text
I 戊
II 庚 ≈ 9/8
bIII 辛 = 6/5
IV 第60步，缺失
V 甲 = 3/2
bVI 乙 = 8/5
bVII 第19步，缺失
VII 丁 = 15/8
```

这接近缺 IV 的和声小调骨架。戊有完整小三和弦 `戊辛甲`，作为属中心的甲又有完整大三和弦 `甲丁庚`。第19步同时是戊的 `bVII≈9/5` 和甲的 `6/5`；丁则同时是戊的导音 `15/8` 和甲的 `5/4`。这使“戊小调主中心 + 甲大调属中心”具有很自然的结构解释，也说明甲缺少自身小三和弦，对它担任戊的属音伤害较小。

癸相对戊为 `7/5`，与戊、辛构成紧凑的 `5:6:7`。从 `戊辛甲=10:12:15` 到 `戊辛癸=10:12:14`，顶声由 15 移到 14，适合形成有辨识度的小调色彩。相比之下，在大调核心中把 `7/5` 与 `1、5/4` 组合会得到 `20:25:28`，结构明显更复杂。因此 `7/5` 更容易被完整小三度环境激活；它在大调中仍可作为变化色彩，不应绝对禁用。

## 6. 108-EDO zigzag 八声音阶校验

`zigzag_108` 的音级是：

```text
{0, 7, 24, 35, 52, 63, 80, 87}
```

108-EDO 中3音七音组相对偏移为：

```text
{0, 24, 35, 45, 63, 80, 87}
```

交集至少四音的候选中心为：

| 3音中心 | 是否在音阶内 | 交集数 | 交集 |
|---:|---|---:|---|
| 0 | 是 | 6 | 0、24、35、63、80、87 |
| 28 | 否 | 4 | 0、7、52、63 |
| 52 | 是 | 4 | 7、24、52、87 |
| 63 | 是 | 4 | 0、35、63、87 |
| 80 | 是 | 4 | 7、35、52、80 |

全部 108 个候选中心的交集数量分布为：

```text
0音:83, 1音:9, 2音:8, 3音:3, 4音:4, 5音:0, 6音:1
```

这个例子说明同一公式可以直接泛化到任意 EDO、任意低限结构音阶，并且必须允许中心位于音阶外。

## 7. 面向代码的评分框架

后续实现不宜把一个交集计数直接命名为“中心强度”。建议先输出可解释的特征，再用可配置权重组合：

```text
coverage       = |field ∩ scale| / |field|
center_present = 0 or 1
kernel_3       = K3 的命中情况
kernel_15      = K15 的命中情况
activation     = 实际成立的紧凑低限子结构列表
missing        = 缺失关系及其结构影响
pair_coverage  = 两个候选场的并集覆盖率
pair_overlap   = 两个候选场的共享枢纽音
directionality = A场是否包含B中心、B场是否包含A中心
```

建议的数据对象至少包含：

```json
{
  "center_kind": 3,
  "center_pc": 0,
  "center_in_scale": true,
  "field_pcs": [],
  "intersection_pcs": [],
  "missing_pcs": [],
  "coverage": 0.0,
  "kernel": {
    "ratios": ["1/1", "5/4", "3/2"],
    "present": [],
    "missing": [],
    "complete": false
  },
  "activated_structures": [],
  "weak_extensions": [],
  "excluded_extensions": [],
  "perceptual_prior": 0.0
}
```

旋律与和声联合评分可以围绕当前场中心展开：

- 首拍、重拍和长音对当前中心判断权重最高。
- 非重拍短音允许作为邻音、经过音和下一中心的预示。
- 当前场特征音提高场置信度；共享音保持歧义并利于切换。
- 另一场特征音连续出现，尤其落在结构位置时，增加中心切换概率。
- 和弦优先选择能激活当前核心及外围低限结构的成员，而不是只最大化和弦音覆盖。
- CSE 仍用于评价实际发声；音场匹配是额外的功能色彩层，不能取代 CSE。

## 8. 与现有代码的边界和实施顺序

现有和弦级3音引擎继续使用和弦自身约分比例，将和弦分为：

1. 和弦内有3音（`present`）。
2. 和弦内无、音阶内有3音（`implied`）。
3. 音阶内无3音（`outside_scale`）。

音阶场引擎应作为新层加入，不应改变这三个状态的含义。推荐实施顺序：

1. 写纯函数生成任意 `center_kind`、EDO 和 patent val 下的诱导场。
2. 为3音和15音分别输出交集、缺失、核心与激活结构。
3. 用本文中的 72-EDO 和 108-EDO 表格建立固定测试向量。
4. 加入双场覆盖、共享枢纽和方向不对称分析。
5. 先以诊断字段接入 IR，不改变生成结果。
6. 用人工旋律与人工和弦统计标定权重，再逐步参与和弦猜测和进行生成。
7. 保留 legacy 与当前和弦级3音模式，便于消融和试听比较。

所有“强”“稳定”“不合适”等词目前都是待作品与试听继续验证的音乐性假设。确定无疑的部分是整数集合、val 映射、交集、缺失和低限比例结构；生成权重应保持可配置并输出诊断依据。
