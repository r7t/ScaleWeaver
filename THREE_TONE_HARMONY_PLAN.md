# 3音引擎 v2 与功能和声

本文记录已经接入的和弦级3音引擎。后来形成的音阶级3音场、15音场、天干甲—辛双中心及强中心判据，见 [3音场与15音场理论备忘录](THREE_FIFTEEN_TONE_THEORY.md)。

采用每个和弦自身约分后的局部比例，通过当前 EDO 的 patent val 推导第三谐波音级。结果始终保留为 `three_tone_pc`（0 至 edo-1），不要求它属于音阶，也不要求实际发声。“3音”不是传统和弦三度。

## 三类和弦

| 状态 | 含义 | 天干数量 |
|---|---|---:|
| present | 和弦内有3音对应的 EDO 音级 | 26 |
| implied | 和弦内无，但音阶内有 | 5 |
| outside_scale | 音阶内无 | 1 |

辛甲己 `4:5:7` 属第三类，3音是 **72-EDO 第19步**，有完整分组与功能分析，不再是无法分类的特殊和弦。

三类覆盖所有有效解释。`unclassified` 只表示缺少比例、比例与音高不匹配、val 不支持其素数等数据问题，不是第四类音乐性分类。当前沿用项目的 17-limit patent val；更高素数需要扩展映射。

## 计算与数据语义

验证各音的 `p_i - val(n_i)` 模八度相同，取共同值加 `val(3)` 并模 EDO，得到3音音级。不能逐个四舍五入比值的 cents 来替代此映射。

整数去掉全部因子2后为3时，`three_tone_explicit_harmonic=true`。若没有这种整数，但 val 将3音映射到已有和弦音级，仍归 present，原因为 `tempered_alias`，显式谐波标记为 false。分类遵循 EDO 音级包含关系，同时保留调律重合与实际谐波的区别。

转位、共同倍乘和八度重复不改变同一解释的分类；移调平移3音音级。调式主音变化只改变相对功能。同音集存在多个 JI 解释时，按最大整数、整数和、误差及确定性顺序选择，不按所需功能挑解释。

`max_integer` 只限制自动生成资格。超过上限仍保留3音、类别和功能，另标 `three_tone_eligible=false` 及排除原因。

## 天干完整分组

“隐含”表示第二类。其他和弦除第19步组外均为第一类；名称顺序沿用原比例表。

| 3音 | 数量 | 和弦 |
|---|---:|---|
| 庚（42） | 7 | 庚甲丁、甲丁壬（隐含）、甲庚壬、丁庚壬、丁庚癸、甲丁庚壬、甲丁庚癸 |
| 甲（0） | 8 | 戊甲丙、甲丙庚、甲丙壬、甲戊庚、丙戊庚（隐含）、丙庚壬（隐含）、甲丙戊庚、甲丙庚壬 |
| 辛（49） | 8 | 戊辛甲、辛乙戊、乙戊癸（隐含）、乙辛癸、戊辛癸、辛癸己、乙戊辛甲、乙戊辛癸 |
| 乙（7） | 4 | 乙丁辛、乙丁癸、丁辛癸（隐含）、乙丁辛癸 |
| 己（35） | 3 | 丙己壬、己癸丙、癸丙己壬 |
| 癸（65） | 1 | 癸丁己 |
| 音阶外第19步 | 1 | 辛甲己 |

自然大调仍为 I/iii 同组（第五级音）、IV/vi 同组（主音）、ii 为第四级音组、V 为第二级音组。减和弦及其他结构按其真实 JI 解释计算。

## 功能和声

所有组，包括音阶外组，都按相对主音的位置计算 T/S/D 亲和度。参考比例经当前 val 映射，再按音高距离连续加权：

- T：主参考 3/2，弱色彩参考 5/4。
- S：1/1、4/3、8/5、16/15。
- D：9/8、15/8、45/32、16/15。

亲和度是独立评分，不是归一化概率。最强方向不足阈值时标 C；音阶外组不因其类别被强制标 C。以甲为主音，庚组为最强 T，甲组为 S。第19步组当前为 C，同时保留具体方向评分与连接关系。

三类的主功能支持系数依次为 1.0、0.75、0.45，基础张力为 0、0.2、0.55，再加离开主参考的张力。这些是待试听评估的作曲偏好，不是分类公式证明的听觉规律。

进行先选组，再选组内和弦。组间结合目标功能、五度链运动与张力释放：第19步也参与五度链，例如 19→49 是下降五度的音级关系。回到显式主参考有额外释放奖励。组内继续使用公共音、声部移动距离、重复抑制、CSE 和大小三和弦色彩。

第二、三类以权重进入正常采样，不再通过随机插入特殊和弦的旁路。终止优先主音上方五度组中含主音的和弦，按 present→implied→outside_scale 优先，再优先三音和弦。缺少完整主参考时使用含主音的合格后备和弦，导出 `three_tone_cadence_fallback=true`；无合法落点则报错。

## 启用和配置

使用 `--harmony-model three-tone` 启用 v2。原有 legacy 模式完整保留并仍为默认。人工进行优先，只附加新分析。联合前端保留完整组路线与终止，伴奏层接收终止主音。

配置位于 `harmony.three_tone_progression`：

| 参数 | 默认 | 含义 |
|---|---:|---|
| enabled | false | 启用3音引擎 |
| max_integer | 64 | 自动生成的比例复杂度上限 |
| outside_scale_weight | 0.25 | 第三类的相对组采样权重；0 禁用其自动选择 |
| implied_weight | 0.8 | 第二类的组采样权重 |
| resolution_weight | 1.2 | 五度连接与张力释放偏好 |

旧 special_weight 接受为 outside_scale_weight 的别名，语义改为相对权重而非插入概率；同时提供冲突值会报错。

导出包含 `three_tone_engine_version=2`、3音音级、三类状态、在和弦/音阶内标记、显式谐波标记、相对音级、功能亲和度、主功能强度、张力、比例及来源、生成资格与排除原因。自动片段 function 使用新标签，legacy_function 保留旧标签；人工片段原 function 不变，另有 three_tone_function。旧导出文件不会自动迁移，重新生成会得到 v2 数据。

验证覆盖三类边界、音阶外 val 计算、移调与八度不变性、调律别名、功能参考与释放方向、第三类参与正常进行、终止、人工进行优先、复杂度过滤保留分类、legacy 输出一致和联合前端保护。音乐效果仍需试听，程序测试与 CSE 不能替代听觉评估。
