# 生成性能优化（2026-09-21）

本次优化同时覆盖旋律联合前端与和声退火，保留评分公式、候选数量、节奏参数、退火步数和随机数调用顺序。没有新增依赖，仍兼容 Python 3.9。

## 实现

- 旋律候选共享同一段历史的音程分布、转向率、音级出现次数与跳进历史。缓存有容量上限，并在切换音阶时清空。
- 退火中复用同一实际配器的纵向评分，包括 sounding、attack 与 harmonic realization；缓存属于单个 Energy 实例，避免不同音阶和配置之间串用。
- 将合法音高预计算为位集合，以交集替代逐候选、逐邻居检查；仍按原音高顺序供随机选择。
- 预先绑定评分因子的固定元数据，减少每次试探的类型分支和 Python 音高收集循环。
- Prime salience 在搜索时只计算非零权重的特征，完整诊断仍计算全部特征。

这些优化以有限的额外内存换取速度：新增纵向评分缓存最多 100000 项，三种旋律历史缓存各最多 8192 项，合法音高集合大小取决于事件数和音域。

## 48 小节旋律前端实测

基线为 `e8a2eb5`。同一环境，天干 72 EDO 五声部日常配置，未启用 cProfile；每组先运行基线、再运行优化版。这里只统计前端，排除音阶/CSE/JI 初始化、伴奏退火和音频渲染。

| 模型 | seed | 优化前 | 优化后 | 加速比 |
| --- | ---: | ---: | ---: | ---: |
| three-tone | 42 | 18.19 s | 7.58 s | 2.40× |
| three-tone | 626 | 12.54 s | 5.03 s | 2.50× |
| legacy | 42 | 46.64 s | 15.62 s | 2.99× |
| legacy | 626 | 32.27 s | 11.94 s | 2.70× |

四组完整前端 IR 的 SHA-256 均与基线一致，包含旋律、节奏、和声与计划信息。以上是少量固定种子的实测，不是所有音阶或机器的保证；缓存冷热状态也会影响结果。

## 完整生成实测

同样使用天干五声部日常配置，three-tone、seed 42、8 小节、750000 步退火；两个独立进程顺序运行，已有磁盘缓存，未启用 cProfile，不含 WAV/MSCX 输出。

| 阶段 | 优化前 | 优化后 |
| --- | ---: | ---: |
| 音阶及缓存初始化 | 15.49 s | 11.89 s |
| 旋律联合前端 | 2.58 s | 0.92 s |
| 退火优化调用 | 61.59 s | 24.74 s |
| 完整生成（含其他开销） | 80.64 s | 38.21 s |

本次退火约加速 2.49 倍，总耗时减少约 53%。初始化代码没有改动，其耗时差异不能归因于本次优化；排除初始化后，生成耗时从 65.14 s 降至 26.32 s。

完整 Score JSON 除 `annealing.elapsed_seconds` 外逐项一致：包括全部声部、节奏、和声计划、最终能量、接受步数、退火历史与诊断。最终能量均为 `4.851560926727956`。回归测试另外对照了 2000 次候选提案及随机数状态、稀疏评分与完整公式、旋律历史统计与原公式，以及评分因子在替换音高列表后的行为。

## 复测

```bash
# 仅测旋律联合前端，单独报告初始化时间
python3 benchmark_generation.py --frontend-only --bars 48 --seed 42
python3 benchmark_generation.py --frontend-only --bars 48 --seed 626 --harmony-model legacy

# 完整生成；沿用风格文件中的退火步数，不渲染音频
python3 benchmark_generation.py --bars 8 --seed 42

python3 -m unittest discover -q
```

可通过 `--scale`、`--rules`、`--style`、`--cse-dir` 指定其他配置。跨版本比较时保持配置、缓存和 Python 环境相同，将本基准脚本复制到基线版本运行；对比时间与输出哈希。首次建表不应与已有缓存时的运行混为一谈。大型 JI 表的首次加载仍有明显成本，本次没有改变其存储格式。
