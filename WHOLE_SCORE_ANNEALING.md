# 按同时起音音数单独设置 attack 权重

本轮补丁覆盖上一版“连续同音惩罚与质数奖励”程序，只含本轮修改文件。

在现有风格 `.style.json` 的 `annealing.attack` 中增加：

```json
"cardinality_weights": {
  "2": 0.25,
  "3": 1.0,
  "4": 1.0
}
```

- 按该时刻**实际同时起音总数**区分；延续音不计入这个总数。
- `objective_weights.attack` 仍为总权重（默认 2.0）。三类有效系数分别为总权重乘上述倍率，默认 0.5、2.0、2.0。
- 倍率乘完整 attack 代价：修正混合 CSE ＋ 百分位超限惩罚 − 质数奖励。设为 0 可关闭该类起音组的优化代价；相应百分位诊断仍照常报告。
- 三音/四音 attack 内的二音子集仍按原百分位标准检查，使用所属整组的倍率；不会再乘“二音同时 attack”的倍率。因此降低二音项不会削弱三音、四音组的子集检查。
- 总 attack 能量继续除以全曲所有多音起音组数，没有按各类重新归一化。三项全设为 1.0 恢复上一版评分。
- 初始化与退火共用此入口。实际 sounding、背景场、voice-leading、旋律生成和最终原始指标统计均不因此改变定义。
- 默认二音倍率降为 0.25，三音和四音为 1.0。旧配置缺省此字段时也采用这组新缺省值。支持非负有限数，包括零；拒绝负值、布尔值、NaN/Inf 和非 2/3/4 键。

本轮只做 14 项针对性与已有回归测试，未复跑 48 小节验收或评价听感。下文各项数值是新增独立倍率前的历史结果，不能当作本轮新默认配置的成绩。降低二音 attack 偏好不保证消除纯八度/纯五度，因为 sounding 和背景场仍会影响选择。

---

# 连续同音惩罚与质数奖励更新

本轮增量补丁基于已经交付的全曲退火版。先安装 `ScaleWeaver_whole_score_annealing_patch.zip`，再将本补丁解压覆盖工程根目录。本包只包含本轮修改的源码、风格 JSON、测试和本说明；不含缓存、音源及试跑结果。

## 本轮修改

`annealing.voice_leading.repeat_cost` 从 **0.52 提高到 2.6**，共 5 倍。代码缺省值与九份风格 JSON 均已同步。贪心初始化与退火统一使用此值，只约束 bass/inner/counter，Lead 保持原生成流程。

“连续同音”是同一声部相邻事件的绝对步数相等。AA 罚一次，AAA 罚两次；跨八度同音名不算相同音高。它是软代价，不禁止有意义的重复。退火中，该项乘 `objective_weights.voice_leading`，并按三个伴奏声部的总相邻连接数归一化。改音会同时更新与前音、后音的连接及有关 n-gram 窗口。

现有风格 JSON 顶层 `prime_rewards` 已接入贪心初始化和迭代优化：支持 2、3、5、7、11、13、17；正值奖励，负值惩罚，0 关闭对应质数。各风格原有质数权重均保留（提供的默认配置均为 0），没有擅自指定偏好的质数。

使用当前音阶的 canonical JI 表，按完整实际音响的约分整数比例判断质数是否出现；每组音响中每个质数最多奖励一次，不因指数、包含该质数的整数个数或子集个数重复加奖。超出表覆盖的宽音域配置，沿用原程序的二音质数集合并集回退。实例缓存音程组合的奖励，不在每次迭代重新分解整数。

令 `R(X)` 为该实际音响所含质数的奖励之和：

```text
sounding 局部代价 = D_corrected(X) + 原八度惩罚 - R(X)
attack 局部代价   = D_corrected(X) + 原百分位超限惩罚 - R(X)
```

分别保持 sounding 的时长平均、attack 的起音组平均，再乘各自总权重（默认 6 和 2）。因此正值奖励的力度也受这两个总权重影响。它归入现有两项，没有新增第五项指标，也没有叠加旧 runtime 的 future_attack 评分。

和弦虚拟背景场不加入质数奖励。非递归最优子集扣除、百分位阈值与最终输出的原始 CSE/CCSE/SE/blending 均不扣质数奖励，避免把奖励当成频谱质量改善。改变质数奖励只会影响伴奏选择，不改 Lead。

例如，在现有 `.style.json` 中修改这两个位置（保留其他键）：

```json
"prime_rewards": {
  "2": 0.0, "3": 0.0, "5": 0.0, "7": 0.1,
  "11": 0.0, "13": 0.0, "17": 0.0
}
```

```json
"annealing": {
  "voice_leading": {"repeat_cost": 2.6}
}
```

## 本轮 seed=666、48 小节验收

4/4、96 BPM、允许十六分音符，混合系数 1/0/0，20 万次退火提案尝试、2 轮下降扫描：

| 配置 | 伴奏连续同音对 | 占相邻连接比例 | 原始四音 sounding CSE | 同时起音组含质数 7 |
|---|---:|---:|---:|---:|
| 上一版，repeat_cost=0.52 | 99/546 | 18.13% | 1.651099 | 见独立校验 JSON |
| 本版默认，repeat_cost=2.6，质数奖励全 0 | 61/519 | 11.75% | **1.699410** | 52/249 = 20.88% |
| 本版，另设 prime_rewards.7=0.1 | 57/519 | 10.98% | **1.724039** | 82/249 = 32.93% |

两次新版完整生成均满足 CSE <= 1.75。baseline.py 独立重算结果相符；两次的 338 个 Lead 完整事件都与上传原程序基准完全一致，最终规则检查全为零。质数奖励例是额外验证，不是默认风格。

本轮默认与质数奖励例的各声部节奏相同。上一版初始化经过三次合法性尝试，本版两次运行均第一次成功，因此上一版与本版的伴奏事件数量不同；99→61 是完整生成结果对比，比例列同时给出了不同事件数量下的情况。每次退火内部仍冻结其初稿节奏。

新增四项针对性测试后共 **12 项通过**：连续同音归一化、canonical 质数奖励与正负/零权重、最终统计/百分位/背景场不受奖励污染、非零奖励下的增量全量一致性及退火 Lead 冻结。原八项回归继续通过。

试跑结果另包提供，包含默认与质数 7 奖励例的 JSON、乐谱、日志和独立校验 JSON。权重变化使原始 CSE 略有上升，是本次实测的取舍；验收只针对上述配置，不承诺任意奖励强度都满足 1.75。

---

# 首版全曲退火实现与验收记录

以下数字记录上一轮交付；本轮结果和新增语义以上文为准。

本补丁基于用户上传的 ScaleWeaver2.zip，保留此前添加的人工和弦进行支持。解压覆盖工程根目录。仅包含本轮变更/新增源码、测试和风格配置，不含 SF2、频谱二进制缓存、未修改的工程模块。

## 已验证的目标

```bash
python main.py --scale scales/tiangan_72.json --seed 666 --bars 48 --cse-dir scales --output output/666.json
```

- 默认 4/4、96 BPM、允许十六分音符、CSE/SE/CCSE 系数 1/0/0。
- 原版 seed=666、best-of=1：四音 sounding CSE = 1.7655614829454602。
- 新版贪心初稿：1.6944446942137734。
- 全曲退火最终：**1.6510989596751922 <= 1.75**。
- 原版 baseline.py 独立重算：1.6510989596751926（浮点末位差异）。
- 338 个 Lead 音符事件全部字段逐项相同，包括音高、起音、时值、力度、相位和附加标记。旋律哈希：`f9ceada3be59bf1307b8fd67b262f5ee52aee349e6ae7dc797b855521d0e571c`。
- score_builder.py 原有函数中只有 `_choose_counterpoint_pitch` 增加初始化器入口；所有旋律函数 AST 均未修改。
- 退火冻结全曲所有音符起音和时值，只改变 bass/inner/counter 的音高字段。
- 主旋律只生成一次。贪心初始化第 3 次伴奏尝试成功，伴奏种子 2000672；前两次失败没有重生成旋律。该尝试是合法性救援，选择第一个成功初稿，不做 best-of。
- 20 万次提案尝试、168941 个非空合法提案、5054 次接受，其中 2263 次接受暂时变差的状态。最后执行 2 次单音下降扫描。最终 391 个伴奏事件音高与贪心初稿不同。
- 8 项最终规则检查为零，三个伴奏声部相邻跳进也均满足原配置硬上限。
- 完整命令复跑两次得到了同样的音高与指标；本环境一次退火约 25 秒，首次频谱与 JI 建表另计。

此数值是上述明确配置和种子的验收结果，不是对任意种子、音阶、参数或缩短搜索预算的保证。没有在搜索里硬编码种子 666、1.75 阈值或对应旋律/音高。

## 最终输出口径

全部仍为扣除单音基线后的原始 SE/CSE/CCSE，**没有扣除最优子集**。

| 对象 | CSE | CCSE | SE | blending |
|---|---:|---:|---:|---:|
| 完整四音 sounding | 1.651099 | 1.769732 | 1.082418 | 1.651099 |
| 恰好二音同时 attack | 0.717756 | 0.764791 | 0.485586 | 0.717756 |
| 恰好三音同时 attack | 1.396562 | 1.509322 | 0.895448 | 1.396562 |
| 恰好四音同时 attack | 1.445496 | 1.537396 | 1.031848 | 1.445496 |

四音 sounding 按持续时间平均，覆盖全部 192 拍，446 个稳定音响时间片。attack 分别按恰好 2/3/4 音起音的时刻等权平均，共 123/72/58 组；不把四音起音中的二音、三音子集混进最终二音、三音统计。缺少某种起音时返回 null/NA，不伪造为零。

保存于输出 JSON 的 `final_spectral_metrics`。`initial_spectral_metrics` 只是最终文件中的初稿诊断，不参与优化。不能把退火能量或扣子集后的内部数值当作最终 CSE。

## 内部修正：严格非递归

令 `D(X) = A*CSE(X) + B*SE(X) + C*CCSE(X)`，其中 D 为原始混合分数：

```text
D2_corrected(X) = D(X)
D3_corrected(X) = D(X) - r3 * min(D(Y) for Y in X 的所有二音子集)
D4_corrected(X) = D(X) - r4 * min(D(Y) for Y in X 的所有三音子集)
```

默认 r3=r4=1/3。四音扣除的是原始混合三音分数，不是 D3_corrected；改变 r3 不会改变同一个四音配置的 D4_corrected。“最优子集”在混合分数上取最小值，不分别挑三个指标的最小值再混合。

这套修正用于贪心伴奏初稿和退火中的三音/四音音响、起音和背景场。Lead 原始生成流程保持原来的未修正评分，这是保持同种子旋律不变的必要边界。

三音背景和弦加候选音形成四音，直接用 D4_corrected。四音背景和弦加候选音时，平均所有“包含候选音＋三个背景音”的四音子集，避免出现未经定义的五音修正路径。重复音按多重集合保留。

## 四项能量

```json
"objective_weights": {
  "attack": 2.0,
  "sounding": 6.0,
  "background": 1.0,
  "voice_leading": 0.35
}
```

- attack：每个至少二音同时起音组的修正分数，加六类百分位软超限惩罚，再按起音组数平均。
- sounding：修正后的实际音响分数，按全曲持续时间平均；不足四部时使用对应音数的修正分数。
- background：三个可移动声部与固定和弦背景场的修正分数。跨和弦边界的长音按各背景段覆盖时长计算；按全曲时长和三个声部归一化。
- voice_leading：步进/跳进代价、连续同音、当前 n-gram 表中的 2–5 gram 代价，以及声部音域中心偏离。跨小节与跨原八小节边界的 n-gram 和相邻音连接照常计算。

旧八度过度重叠惩罚归入 sounding 项，`sounding_octave_excess_cost` 默认 1.0，可以设为 0。每个时间片统计 1/2/3 个八度的音对数 N，超过一对时增加 `cost*max(0,N-1)`。不是音级多样性奖励；没有音集大小、熵或等效音数优化项。

旧 `cse_weights.COUNTERPOINT_*` 不再控制新优化器，包括旧 ACTUAL、独立四部主项及旧相对纯度叠加项。提供的风格文件已移除这些键；若用户自己的风格还含有这些键，正式入口会提示迁移。`cse_weights` 中 A/B/C 和三个 LEAD_CHORD_FIELD 参数继续服务原旋律生成；A/B/C 同时决定优化器的基础混合指标。

### attack 软上限

保留原六类百分位阈值，但百分位现在对**对应音数的修正混合分数全集**重新计算，不能复用旧纯 CSE 或未修正混合分数的百分位。

为了形成与提案集合无关的固定状态能量，旧“最小超限＋fallback_slack”候选筛选不进入退火。`fallback_slack` 暂保留在旧配置结构中供兼容加载，但不影响新优化器。新惩罚由以下字段控制：

```json
"attack": {
  "percentile_excess_weight": 15.0,
  "percentile_excess_squared_weight": 30.0
}
```

对一组 attack，枚举适用的六类子集检查，令 e=max(0,实际分位数−阈值)。该起音组代价为：

```text
D_corrected(起音组) + 15 * max(e) + 30 * mean(e²) - R(起音组)
```

所有声部交叉、狼音、普通小间距和相邻最大跳进等硬约束不允许退火绕过。attack 是软惩罚，无法保证零超限；本验收曲修正百分位下 1049 次子集检查有 6 次超限，详情在 `simultaneous_attack_cse_limits` 中。

## JSON 参数位置

完整 `annealing` 对象已写入提供的每一份 `.style.json`，共九类设置：

- `enabled`：false 时只输出新的贪心伴奏初稿。
- `subset_correction.triad/tetrad`：非递归扣除倍率。
- `objective_weights`：四项总权重。
- `attack`：软超限线性最大值及均方惩罚权重。
- `background_voice_weights`：counter/inner/bass 的背景权重。
- `voice_leading`：运动、重复、额外大跳、各声部 n-gram、低音专用 n-gram、音域中心权重。
- `sounding_octave_excess_cost`：过多八度音对惩罚。
- `initialization`：最大伴奏合法性尝试次数、确定性种子间距。只改变伴奏随机流。
- `search`：退火步数、首末温度、1/2/3 音提案比例、局部提案概率与级数半径、最后下降扫描次数、进度间隔。

缺省值也集中在 annealing_config.py。旧风格未提供 annealing 时采用这些默认值。数值、类型和未知键在配置加载阶段验证。

## 提案与复现

- 从全曲伴奏起音组中抽样；一次改变一、二或三个不同伴奏声部中同一时刻起音的音符。
- 单音/双音/三音提案默认权重 0.6/0.3/0.1；实际改变数可能小于抽中的数量，因为部分音可能抽到原音高。
- 随机提案首先受硬约束过滤，局部与全域候选混合；采用温度下降和 Metropolis 式接受。这是用于找低能量状态的启发式退火，不宣称对不对称提案进行了严格的平衡分布采样。
- 同一输入和同一参数可复现。温度、步数和权重变化不会改变 Lead；它们可以改变最终伴奏。
- 缓存时间片、起音组、声部相邻关系和每个音影响的评分项。一次改音只更新关联因子；所有被改音持续区间和 n-gram 后继窗口均被覆盖。
- 最终恢复历史最低能量状态后再做有限下降扫描。优化能量不劣于初稿，但单项原始 CSE 或单项 attack 均值不保证逐项下降。
- 原整曲 best-of 已退休；旧 CLI 参数仍能解析，但取值大于 1 时提示忽略，避免旧脚本突然无法调用。
- 为保证旋律和节奏冻结，旧 pitch-changing cleanup/legacy optimizer 不再允许执行，也没有末尾删除同度音的步骤。严格声部排序在生成/提案时防止同度。

## 使用与验证

有原 SF2 时：

```bash
python piano.py 666 --scale scales/tiangan_72.json --bars 48
```

没有 SF2 时加 `--no-wav`。第一次为音阶运行时自动生成缺失缓存。piano.py 顶部默认音阶路径仍沿用此前配置，请使用明确的 --scale。

```bash
python tests/test_annealing.py -v
```

8 项测试通过：非递归和零倍率、混合权重、修正百分位分布、增量/全量一致、1/2/3 音同起音提案、硬约束和 Lead 冻结、复现性与历史最佳保留、最终原始统计及参数校验（部分合并在同一测试方法）。测试需要对应频谱缓存；缺失时会自动建立。

31-EDO 4/4 与 19-EDO 3/4 的 7 小节、2000 步短测均通过，4536251 循环保留。12/19/31 的补丁只更新风格 JSON，音阶定义、规则、n-gram 使用之前交付的对应文件。

示例包包含原版与新版乐谱/JSON、原旋律基准、独立校验、测试记录及新版原生合成 WAV。WAV 直接使用准确音高，未使用缺失的 SF2，不代表用户 SF2 的最终音色。
